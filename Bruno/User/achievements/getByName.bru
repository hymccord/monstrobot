meta {
  name: getByName
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/user/{{user}}/achievements/star
  body: none
  auth: none
}

body:json {
  {
    "hgToken": "{{hgToken}}",
    "uniqueHash": "{{uniqueHash}}"
  }
}

vars:pre-request {
  user: 1
}
