meta {
  name: CheckRole
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/role/1/star
  body: json
  auth: none
}

body:json {
  {
    "hgToken": "{{hgToken}}",
    "uniqueHash": "{{uniqueHash}}"
  }
}
