meta {
  name: Get all roles by ID
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/role/star/4615936
  body: json
  auth: none
}

body:json {
  {
    "hgToken": "{{hgToken}}",
    "uniqueHash": "{{uniqueHash}}"
  }
}
